<!-- stem -->
Always work through the stem skill: size the work, keep
docs/planning/ current, verify before done.
Now: The jury demo (caller's screen + front desk screen; demos Sunday 20 Sep 11:00–13:00) — agreed by Mark 03:30; the repo is a monorepo since 03:50 (agent in apps/agent, web app in apps/web; .venv, .env, logs, docs stay at the root). Next: the exact piece in docs/planning/active/demo-web/work.md. Until the wall freezes (06:00) never restart the phone server (:7860) or stop the scored loop (started as scripts/scored.py loop, still running); the agent's code reaches the phone line only through apps/agent/scripts/restart.sh, and only with Mark's yes. Parked: Clinic voice agent — 172 points, the maximum (four teams tied); ElevenLabs voice "Elise" live since 03:12; what is left is in docs/planning/active/clinic-voice-agent/work.md. Standing decisions: commit after each verified piece (no pushes), no full text-eval runs unasked.
<!-- /stem -->
