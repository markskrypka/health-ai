# secrets — never commit
.env
.env.*
!.env.example

# python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# per-call event logs and recordings (simulated patient data — keep out of git)
logs/

# dependencies and build output
node_modules/
dist/
.next/
.turbo/

# local noise
.DS_Store
*.log

# raw eval results (large; the summary is what gets shown)
eval-runs/
