# docs/planning

Planning docs kept by the stem skill: `worklog.md` (every change, newest first), `active/` (initiatives in motion), `research/` (standalone probe notes), and `archive/<year>/` (accepted work).
