# Work — Clinic voice agent

**State (2026-09-19, ~19:45 Madrid) — 148 points, rank 1, every open problem 4/4; the board is now decided by problems 15–18.**
- Banked since 18:10: noise, languages, no_slot_free, change_and_cancel and a new problem, "wild_card" (weight 3, 7 of 8 calls passed). A scored run now dials **four calls at once**; `scripts/scored.py` logs only the first case's verdict (read the rest with `scored.team(http)["runs"]`). The loop idles until a problem opens. Credited passes cannot be lost, so code changes now risk nothing on the board.
- Read of the leader's repo: `research/2026-09-19-competitor-cachopo.md`. Their lead was calls banked, not a better agent; our write path (replace, POST at hang-up) and our time discipline are ahead; their code is stricter on later moves, slot-to-patient binding, refusal evidence and nearest site.
- Deployed 19:35: the caller id always counts as evidence and a sound id forgives a mangled name (scored languages call, "Alys Davis").
- **Exposed before 15–18 open (60 points):** the reschedule path has never run live (all four change_and_cancel scored calls were cancels) and the_real_call leans on it: the move guard misses "cannot make his appointment"/"no va a poder ir", doctor and site are not pinned on a later move, and once one action is recorded the wrap-up clock, the nudge and the hang-up fallback all switch off. second_policy: a caller who cannot name the plan. nearest_site: the model names the site itself in 8 of 12 eval runs, so the geocoder never runs. One no_slot_free miss: "what's the next one?" — it keeps the clinic, not the doctor (fixed 20:20, see below).
- Deployed 20:20: a later move keeps its doctor and clinic and is recognised as a move; "the next one" keeps the clinic (any doctor); no goodbye while something asked for is open. Text evals on the touched cases green (the_real_call 3/3, later move 1/1, no_slot_free 4/4 today); not yet through the harness.
**Next action:** one practice call on a published the_real_call case and one on the published later move (`change_and_cancel-78692f3782fd`) — the queue is free while nothing is owed; then the rest of the research note's order — (1) the_real_call, second half: the wrap-up clock, the nudge and the hang-up fallback keyed on an open offer rather than on "nothing recorded"; (2) second_policy prompt sentence + bill only a plan the caller said; (3) a nearest_site step before identification; (4) the small ones (slots bound to their patient, emergency latch, one-letter doctor names, phone and email shapes, one retry on platform GETs, every case of a scored run in the loop's log); then jury material.
**Waiting on Mark:** nothing. Still open from 18:25: run `scripts/night.sh` in his own terminal so the night does not depend on the assistant's session.

**State (2026-09-19, ~18:25 Madrid).** 99 points once the "Noise" pass lands (17 of 18 scored calls passed). The platform's queue is 10–15 minutes per run this evening. The loop (scored calls only, line check) runs in the assistant's session; **Mark should run `scripts/night.sh` in his own terminal so the night does not depend on that session.** Call console: `.venv/bin/python -m uvicorn clinic_agent.console:app --port 7870`. Jury material still to do: an eval/verification page ("how we know it works"), a demo script, ten calls at once, and ElevenLabs as a voice-only option for the jury call.

**State (2026-09-19, ~18:10 Madrid) — 96 points, the loop is banking; language build deployed.**
- Scored so far: 17 calls, 16 passed. Fully credited: problems 1, 3–6, third_party, difficult_caller, adversarial, triage. Owed: languages, noise, no_slot_free, change_and_cancel (4 each). Problems 15–18 are not open yet; the loop picks them up when they open.
- Deployed 17:58: a voice per language, the Catalan listening model, two-exact-details identification, a refusal that takes back the booking it follows. The published Catalan case passed on it (117 s; 236 s on the build before).
- Learned from the platform today: a matching record passes even with a `wall_clock` signal; the cooldown between scored calls is about 5–7 min in practice, not 12; practice calls can sit in the queue for more than 6 minutes (the scripts give up waiting, the call still happens).
**Next action:** let the loop bank the four owed problems; read any failed scored call from `logs/calls/`; then the jury material (live call view, eval report, demo script, ten calls at once), and ElevenLabs as a voice-only option for the jury call.
**Waiting on Mark:** nothing.

**State (2026-09-19, ~16:00 Madrid) — new rules, scored loop running.**
- Rules 2.1: one scored call per problem, 12 min cooldown from the finish, a problem credits its first four passes, calls pool, ties share a rank, wall freezes Sunday 06:00. We hold 40 (problems 1–6 fully credited), 8th; leader 78. Problems 7–10 are open (40 more points); 11–18 not yet.
- `scripts/scored.py loop third_party triage no_slot_free change_and_cancel` runs in the assistant's session → `logs/scored-loop.log`. It owns both lanes (Mark: nobody else dials). First scored call 15:50. Practice on the new problems before it: 6/6 passed.
- To change code under the loop: edit, test, `scripts/restart.sh` (it pauses the loop and waits for the run in flight), commit.
- Follow-up for clarity: `tools.recover_leaked_calls` only serves the text evals now (live, `GuardedGoogleLLM` recovers a written call when it happens); give the eval loop the same guard and delete it.
**Next action:** watch `logs/scored-loop.log`; read every failed scored call from `logs/calls/<call_id>.jsonl`, fix, restart, commit. Then wire languages (`languages.py`: Spanish voice, Catalan listening model) before problem 11 opens; then the jury material.
**Waiting on Mark:** nothing.

**State (2026-09-19, ~12:55 Madrid) — second Run All had 3 failures; submissions now retry; first commit made.**
- Run All 2 (12:09–12:32), from our logs: 22 calls, none lost to a tool call written as text (the guard recovered one live), but 8 submissions got `ReadTimeout` from the platform and two calls ran 185 s and 211 s: the platform's API was answering in 4–6 s per request. Mark reports 3 failed cases. Fix deployed 12:45: `finalize` retries inside the 30 s window (see worklog).
- Committed as `f69cb71` on branch `clinic-voice-agent` (main is the default branch and still has no commit; `git branch -m main` puts it there).
**Next action:** Mark's third Run All is the test of the retry fix; read its failures from `logs/calls/`. Then languages (parked) and problems as they open.

**State (2026-09-19, ~12:00 Madrid) — first Run All 36/40, tied for first; the cause of both misses fixed and deployed.**
- Run All #39 (11:20): 18 passed, 2 failed, both `wall_clock` + `record_mismatch`, one 1-point and one 3-point case. Cause, from our own logs (`logs/calls/72a78c2a…`, `8530e780…`): Gemini wrote its tool calls as text, the agent spoke them, nothing ran, the pattern repeated to the wall. Fixed in `speech.py` (`GuardedGoogleLLM`): such text is never spoken, the written call is parsed and run as a real call, and it never enters the context. Deployed 11:50; smoke call through the harness passed (90 s).
- Same build also carries: replayed replies dropped, half-sentences waited out, nudge at 10 s, wrap-up keeps the offer on the table, chart notes hidden, a booking stays a booking. "When Exactly" #5 and "The Rules" #5, the two practice failures, now pass (115 s and 87 s).
- 70 tests pass, 4 live ones skip (their published slot is in the past). No full text-eval run since these changes — Mark asked to skip them (cost); verification is unit tests plus practice calls.
- Parked at Mark's request: languages. Measured and decided but not wired: English stays on `aura-2-thalia-en` (96% word match on an 8 kHz line vs 86–88% for bilingual Spanish voices), Spanish goes to `aura-2-carina-es` (99%); Deepgram Nova-3 `multi` garbles Catalan dates, Nova-2 `ca` is near-perfect, so a Catalan caller needs a mid-call STT settings switch. `src/clinic_agent/languages.py` holds the detection helpers, unused so far; `tools._language_code` (spoken language name → ISO code) is live.
**Next action:** Mark re-runs Run All; read its failures from `logs/calls/` the same way; then languages and the problems as they open.
**Waiting on Mark:** the Run All result; the go for a first git commit.

**Earlier state below.**

**State (2026-09-19, ~02:50 Madrid) — open problems green in text, New Patient fixed on the real harness.**
- Real harness, practice lane, current code: "The New Patient" **4/4** (all eight fields by voice; calls 94–123 s, down from 150–190 s). Earlier code: "The Simple Booking" 4/4, "The Doctor and the Site" 5/5. Running now on the current code: `when_exactly`, `the_rules`, and a re-check of those two → `logs/practice-*-open-set.log`.
- What the 02:10–02:14 failures on the dashboard were: (a) "When Exactly" `connection_lost` — the assistant restarted the server while its own batch had a call in flight; `scripts/restart.sh` now waits for zero active calls. (b) "The New Patient" `wall_clock`, 43 turns — a read-back loop: the harness's caller compares our spoken email, as its own speech-to-text heard it ("Joaquin Gonzales twenty four"), with its written address and says "no" for ever. Fix: emails are never read back; `repair_email` rebuilds the name part from the captured name; ids are checked by `check_national_id` and read back only when unsound; phone must be nine digits; registration is three questions; a wrap-up clock at 145 s tells the model to record what it has. (c) the three failed "Simple Booking" calls at 00:21–00:39 predate our agent.
- Text evals, full set twice on the current code: **141/146 runs (97%)**, every open problem 100%. Five cases flaked once each, all in problems not open yet (the_questions, no_slot_free, third_party, nearest_site, noise) — to look at next. 53 tests pass.
**Next action:** read the open-set practice results; with Mark's go, the first Run All (5 scored problems open = 20 calls); then the five flaky cases.
**Waiting on Mark:** the go for Run Alls; the go for a first git commit.

**Earlier state (2026-09-19, ~02:15 Madrid) — through the real harness; pieces 1–3 done, piece 4 in progress.**
- Real harness, practice lane (scores nothing): tunnel via plain `ngrok http 7860` (no static domain needed — `scripts/practice.py endpoint` re-registers whatever URL the tunnel has); dashboard endpoint now ours (the old one was a dead quick tunnel). Results on the first code: "The Simple Booking" 4/4, "The Doctor and the Site" 5/5, "The New Patient" 1/4. The three misses: an email that lost its digits in speech-to-text and was read back together with the next question; an insurer the model invented (`privado`) without asking; registration calls running 150–190 s against the 180 s cap. All three addressed (read-back asks only "Is that correct?", email read naturally not letter by letter, insurer guard in code). A second batch on the fixed code is running: `the_new_patient`, `when_exactly`, `the_rules` → `logs/practice-*-fixed.log`.
- Text evals (`.venv/bin/python -m evals.run`, no phone, clock pinned per case, the leaderboard's exact-match rule in `evals/scoring.py`): first full run 61/73 (84%). After tonight's fixes every problem that had failures re-ran green — third_party 8/8, difficult_caller 10/10, the_rules 10/10, the_questions 10/10, no_slot_free 12/12, change_and_cancel 12/12, the_real_call 9/9, the_new_patient 11/12 (the miss: the model wrote a tool call as text; now recovered at hang-up). A full 73-case re-run with repeats is still owed. Text evals say nothing about audio: noise, languages and interruptions need real calls when those problems open.
- 49 tests pass (`.venv/bin/python -m pytest`). Median model round 1.2 s, p95 1.5 s.
- Running in the background from the assistant's session (they die with it): the real server on :7860 under `caffeinate`, the ngrok tunnel, the practice batch. For scored runs Mark should run `scripts/serve.sh` and `ngrok http 7860` in his own tabs, then `scripts/practice.py endpoint`.
**Next action:** read the fixed-code practice results; full 73-case eval with `--runs 3`; then, with Mark's go, the first Run All (problems 1, 3, 4, 5, 6 are open = 20 scored calls).
**Waiting on Mark:** permission for Run Alls; git (nothing is committed yet); who runs server and tunnel for scored runs.

**Earlier state (2026-09-19, ~01:45 Madrid) — piece 1 verified locally, not yet through the real harness.** Built since the entry below: `prompt.py` (conduct rules + facts block generated from the catalogue), `bot.py` (one Pipecat pipeline per socket: Deepgram Nova-3 `multi` → `gemini-3.6-flash` minimal thinking → Deepgram Aura-2, 8 kHz end to end, tools registered, greeting, quiet-line nudge, holding phrase, `finalize` on hang-up), `server.py` (FastAPI `/ws` + `/health`), `scripts/local_call.py` (plays the harness: Twilio-format frames, synthesized patient, transcribes what came back), `scripts/serve.sh` (the real server for Mark's terminal, with `caffeinate`). Three local calls ran clean, no server errors; each captured submission equalled today's accepted answer for the published case (BOOK P00001 · PR03 · sur · review · 2026-09-21T09:00+02:00 · mapfre). 40 tests pass.
Two fixes that came out of those calls: (1) Pipecat's default end-of-turn model split "Hi, I'd like…" and a dictated DNI into several turns → replaced by a plain silence timeout (`TURN_END_SILENCE_SECS = 0.9` + VAD 0.2 s); the DNI now arrives as one turn. (2) ~6 s of silence while two lookups ran → one spoken holding phrase per caller turn when a lookup starts; the agent now answers ~2.4 s after the caller stops. Read in Pipecat's source: its WebSocket output paces audio in real time, so stopping on an interruption works although the harness ignores `clear`.
Measured, single laptop, not a benchmark: a model round is ~1.1–1.5 s; a whole simple booking takes ~65–80 s of the 3-minute cap.
**Next action:** the real harness — needs `NGROK_DOMAIN` in `.env` and Mark's tunnel tab (`ngrok http --url=<domain> 7860`), then `scripts/serve.sh`, then the dashboard endpoint set to `wss://<domain>/ws` (the URL registered there now is a dead quick tunnel) and a practice call on "The Simple Booking". In parallel, unblocked: piece 3, the text-eval runner over `docs/organizers/public-cases.json`.
**Waiting on Mark:** `NGROK_DOMAIN`; answers to the eight inline questions of ~01:10 (endpoint switch, who runs server/tunnel, Run Alls, spend cap, git, unattended mode, other writers, voice). Defaults in force: dashboard untouched, no commit, no Run All.

**Earlier state (2026-09-19, ~01:00 Madrid):** Mark said "Great… let's proceed" to contract v3 and the plan. Built and verified so far: Python 3.13 venv with `pipecat-ai==1.11.0` (all modules the plan needs import cleanly); `src/clinic_agent/` — `config`, `ids`, `dates`, `geo`, `clinic` (API client), `session` (per-call state + JSONL event log), `tools` (the nine agent tools + `finalize`). **40 tests pass**: 23 pure-logic, and 17 published cases driven through the tool layer against the live clinic API with the clock pinned — every one reaches the published accepted answer. Not built yet: the Pipecat bot and `/ws` server, the system prompt, the text-eval runner, the practice-call script.
**Next action:** write `prompt.py`, `bot.py`, `server.py` (piece 1) and `evals/run.py` (piece 3). The first verifiable step of each needs keys.
**Blocked on Mark:** three values in `.env`, all still empty — `GOOGLE_API_KEY` (unblocks the text evals and the LLM), `DEEPGRAM_API_KEY` (hears and speaks; replaces the Soniox key and the Google service-account JSON since the "Fewer services" decision), `NGROK_DOMAIN`; and answers to the seven inline questions of 2026-09-19 (teammate's endpoint, Python env, Run Alls, first commit, voice). Defaults are in force meanwhile: dashboard endpoint untouched, no commit, no Run All.

Learned from the live API (both now handled in `tools.find_slots` and covered by tests):
- A doctor on leave is **not** reported in `blocked`; the API just returns his slots after the leave. The leave rule (same kind of doctor, same site) is applied from the catalogue.
- `/availability` lists slots on the published closure day (Monday 12 October) as it does for today; neither is ever an accepted answer, so both are filtered in code.
- `blocked[].restriction` values are exactly the submission `reason` strings. A named doctor blocked for this patient → redirect within the specialty; a whole specialty blocked → refusal, after asking for a second plan when the rule is a coverage one.
- In Pipecat's Twilio parsing, `from_number` arrives in `call_data["body"]` (the stream's custom parameters), and `call_data["call_id"]` is `start.callSid`.

Hard facts that drive the order: the wall freezes **Sunday 06:00**; a Run All takes ~18 min plus a 15 min cooldown; best run counts; problems 1–6 are open now and the rest open over the weekend; every call is capped at three minutes.

## Plan — one build stream

1. **"A talking agent on the wire"** — Python 3.12 + `uv`, pinned `pipecat-ai`; FastAPI `/ws` with `parse_telephony_websocket` + `TwilioFrameSerializer(auto_hang_up=False)`; Deepgram Nova-3 (`multi`) → Gemini Flash → Deepgram Aura-2; per-socket pipeline; `call_id` and `from_number` read from `start`; ngrok. Done: a practice call connects, the agent greets, hears and answers, and the dashboard transcript reads cleanly. Verify: the recording and transcript of that practice call.
2. **"First pass: the simple booking"** — clinic client with cached catalogue; tools `find_patient`, `find_slots`, `submit_booking`, `submit_no_action`; session state and a JSONL event log; guaranteed submission at call end. Done: the four published "Simple Booking" cases pass as practice calls. Then spend the first Run All — points on the board early.
3. **"Evals without phoning"** — the text-mode runner over `public-cases.json` with the clock pinned, exact-match scoring, repeats, a per-problem report. Done: it reproduces the practice-call verdicts for problem 1 and runs all open problems in minutes.
4. **"The open problems"** (3 The Doctor and the Site, 4 The New Patient, 5 When Exactly, 6 The Rules) — provider-name resolution and leave fallback; registration with check-letter validation and read-back; the fixed date vocabulary in code; `blocked` → `reason`. Done: text evals ≥ 4 of 5 per case; spot-checked with practice calls; Run All.
5. **"The problems still to open"** (7–18), in weight order as they open — no slot free, change and cancel, third party, triage, languages, noise, difficult caller, adversarial, nearest site, the questions, second policy, the real call. Each: cases green in text evals, one or two practice calls, then a Run All.
6. **"Ten at once"** — the Switchboard practice bursts (5, 10, 20); Deepgram and Gemini limits checked under load. Done: a burst of ten passes with no dropped or silent calls.
7. **"Minimal live view and the demo"** — live transcript, tool calls, submissions, timings; eval report with variance and cost per call; personal touches from the chart note and visit history for the jury.

## Follow-ups

Parked — not in the plan unless Mark pulls one in: switching Deepgram to its Catalan mode mid-call; a Catalan (and Galician, Basque) voice; ElevenLabs' voice for the jury's single call; Gemini Live or Soniox as alternative voice parts; cloud hosting for scored runs; Pipecat's own eval tool in audio mode; tracing vendor; analytics.
