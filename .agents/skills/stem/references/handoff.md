# handoff — leave the room ready

Reconcile first: is the worklog current? Does the active initiative's
`work.md` show the true status and the exact next action, right now?
Are decisions recorded and research notes persisted? Sweep `active/`:
anything accepted but not yet archived moves to `archive/<year>/` now,
and every remaining state note must be true today, not merely recent.

Write the state note at the top of the active initiative's `work.md`:
one line for where things stand, one for the next action, one for any
blocker. Regenerate the managed block in the agent files to match —
the next session reads it before anything else.

Run the cold-start check: could a fresh session, reading
`docs/planning/` alone, state the goal, the current status, the
material decisions, and the next action? If anything important lives
only in this conversation, persist it now — or tell the user exactly
what would be lost.

Offer the commit if accepted work sits uncommitted. Then close with one
line: what the next session should do first.
